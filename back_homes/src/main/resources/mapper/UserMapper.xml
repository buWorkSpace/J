<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.homes.mapper.UserMapper">

    <!--회원가입-->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (email, password_hash, phone, name, contract_file)
        VALUES (#{email}, #{passwordHash}, #{phone}, #{name}, #{contractFile})
    </insert>

    <!-- 이메일로 계정 조회-->
    <select id="findByEmail" resultType="com.homes.domain.User">
        SELECT *
        FROM user WHERE email = #{email}
        AND is_deleted = FALSE
    </select>

    <!-- 전화번호로 계정 조회-->
    <select id="findByPhone" resultType="com.homes.domain.User">
        SELECT *
        FROM user WHERE phone = #{phone}
        AND is_deleted = FALSE
    </select>

    <select id="findByUserId" resultType="com.homes.domain.User">
        SELECT *
        FROM user
        WHERE user_id = #{userId}
        AND is_deleted = FALSE
    </select>

    <update id="updateUser">
        UPDATE user
        SET
        name = #{name},
        phone = #{phone},
        password_hash = #{passwordHash}
        WHERE user_id = #{userId}
    </update>
    
</mapper>